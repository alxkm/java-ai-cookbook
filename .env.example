# Pick one provider. Leave the others empty.
OPENAI_API_KEY=
ANTHROPIC_API_KEY=
OLLAMA_BASE_URL=http://localhost:11434

# Optional: force a provider instead of auto-detecting from the keys above
# AI_PROVIDER=openai

# The embedding and RAG recipes (06-09) use this instead, because Anthropic
# has no embedding endpoint. openai or ollama.
# EMBEDDING_PROVIDER=openai

# Recipe 18 only
# OLLAMA_CHAT_MODEL=llama3.2
# OLLAMA_EMBEDDING_MODEL=nomic-embed-text
